# GitHub Pages setup

This project is configured to auto-build and deploy the Godot Web export to GitHub Pages from the `main` branch.

If the first Actions run fails, check the workflow logs for template download or export issues.
